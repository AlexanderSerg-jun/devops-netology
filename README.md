## devops-netology
# В файле .gitignore 
# 1.будет игнорироваться все файлы с расширением *.tfstate (так как * означает любой символ).
# 2.будет игнорироваться все файлы с расширением *.tfvars (так как * означает любой символ).
# 3.будут игнорироваться временные файлы информации о блокировке .terraform.tfstate.lock.info
# 4. будут игнорироваться все файлы переопределения(так как * означает любой символ).
## Все скрины действий находятся в папке [Действия(img)](./img/)
